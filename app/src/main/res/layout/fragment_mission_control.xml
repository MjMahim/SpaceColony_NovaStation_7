<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground"
    tools:context=".ui.MissionControlFragment">

    <!-- ======================================= -->
    <!-- PRE-MISSION LAYOUT                      -->
    <!-- ======================================= -->
    <LinearLayout
        android:id="@+id/layout_pre_mission"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:background="@color/colorBackground"
        android:visibility="visible"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Title -->
        <TextView
            android:id="@+id/tv_mission_control_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:gravity="center"
            android:text="@string/title_mission_control"
            android:textColor="@color/colorTextPrimary"
            android:textSize="22sp"
            android:textStyle="bold"
            android:letterSpacing="0.05" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="8dp"
            android:layout_marginStart="24dp"
            android:layout_marginEnd="24dp"
            android:layout_marginBottom="8dp"
            android:background="@color/colorDivider" />

        <!-- No crew message -->
        <TextView
            android:id="@+id/tv_no_crew_msg"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:text="@string/no_crew_msg"
            android:textColor="@color/colorTextSecondary"
            android:textSize="14sp"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- Squad selection RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_squad_select"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:clipToPadding="false"
            android:paddingBottom="8dp"
            tools:listitem="@layout/item_crew_member" />

        <!-- Launch Mission button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_launch"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:text="@string/btn_launch"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:textSize="16sp"
            app:backgroundTint="@color/colorAccent"
            app:cornerRadius="8dp" />

    </LinearLayout>

    <!-- ======================================= -->
    <!-- IN-MISSION LAYOUT                       -->
    <!-- ======================================= -->
    <LinearLayout
        android:id="@+id/layout_mission"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:background="@color/colorBackground"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Threat Card -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_threat"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="8dp"
            app:cardBackgroundColor="#2A1A1A"
            app:cardCornerRadius="8dp"
            app:cardElevation="4dp"
            app:strokeColor="@color/colorThreat"
            app:strokeWidth="1dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="12dp">

                <!-- Threat Name -->
                <TextView
                    android:id="@+id/tv_threat_name"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/colorThreat"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.05"
                    tools:text="ALIEN VANGUARD" />

                <!-- Threat Stats -->
                <TextView
                    android:id="@+id/tv_threat_stats"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:textColor="@color/colorTextSecondary"
                    android:textSize="12sp"
                    android:fontFamily="monospace"
                    tools:text="ATK: 8  DEF: 4  SPD: 6" />

                <!-- Threat Energy ProgressBar -->
                <ProgressBar
                    android:id="@+id/pb_threat_energy"
                    style="?android:attr/progressBarStyleHorizontal"
                    android:layout_width="match_parent"
                    android:layout_height="8dp"
                    android:layout_marginTop="8dp"
                    android:max="100"
                    android:progress="80"
                    android:progressTint="@color/colorThreat"
                    android:progressBackgroundTint="@color/colorDivider" />

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Squad Status Bar — all members + their energy, updates each turn -->
        <HorizontalScrollView
            android:id="@+id/scroll_squad_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="4dp"
            android:scrollbars="none">

            <LinearLayout
                android:id="@+id/layout_squad_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingTop="4dp"
                android:paddingBottom="4dp" />

        </HorizontalScrollView>

        <!-- Current Actor Card -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_actor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="4dp"
            app:cardBackgroundColor="@color/colorCard"
            app:cardCornerRadius="8dp"
            app:cardElevation="4dp"
            app:strokeColor="@color/colorAccent"
            app:strokeWidth="1dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="12dp">

                <!-- Current Actor Name -->
                <TextView
                    android:id="@+id/tv_current_actor"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/colorTextPrimary"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    tools:text="Cmdr. Reeves (Pilot)" />

                <!-- Actor Energy Label -->
                <TextView
                    android:id="@+id/tv_actor_energy"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:textColor="@color/colorTextSecondary"
                    android:textSize="12sp"
                    tools:text="Energy: 75 / 100" />

                <!-- Actor Energy ProgressBar -->
                <ProgressBar
                    android:id="@+id/pb_actor_energy"
                    style="?android:attr/progressBarStyleHorizontal"
                    android:layout_width="match_parent"
                    android:layout_height="8dp"
                    android:layout_marginTop="6dp"
                    android:max="100"
                    android:progress="75"
                    android:progressTint="@color/colorEnergyGreen"
                    android:progressBackgroundTint="@color/colorDivider" />

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Mission Log RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_mission_log"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="0.4"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="4dp"
            android:background="@color/colorSurface"
            android:clipToPadding="false"
            android:padding="4dp"
            tools:listitem="@layout/item_mission_log" />

        <!-- Action Buttons -->
        <LinearLayout
            android:id="@+id/layout_action_buttons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="@color/colorSurface"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="8dp"
            android:paddingBottom="4dp"
            android:gravity="center">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_attack"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="4dp"
                android:text="@string/btn_attack"
                android:textColor="@color/white"
                app:backgroundTint="@color/colorThreat"
                app:cornerRadius="8dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_defend"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="4dp"
                android:layout_marginEnd="4dp"
                android:text="@string/btn_defend"
                android:textColor="@color/white"
                app:backgroundTint="@color/colorPrimary"
                app:cornerRadius="8dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_special"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="4dp"
                android:text="@string/btn_special"
                android:textColor="@color/black"
                android:textStyle="bold"
                app:backgroundTint="@color/colorAccent"
                app:cornerRadius="8dp" />

        </LinearLayout>

        <!-- Return Crew Button (shown after mission ends) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_return_crew"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="8dp"
            android:text="@string/btn_return_crew"
            android:textColor="@color/white"
            android:visibility="gone"
            app:backgroundTint="@color/colorPrimaryDark"
            app:cornerRadius="8dp" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
